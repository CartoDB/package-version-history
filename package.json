{
  "name": "package-version-history",
  "version": "0.1.0",
  "description": "Find the versions of a package in a local git repository",
  "main": "index.js",
  "scripts": {
    "test": "ava --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.0"
  },
  "devDependencies": {
    "ava": "^0.22.0"
  },
  "ava": {
    "files": [
			"test/*.js"
		]
  }
}
